import pygame as pg
from .state import State

class MenuState(State):

    def __init__(self):
        super().__init__("menu_state")

    def load_buttons(self):
        buttons = {}

        return buttons

    def load_text(self):
        text = []

        return text

